# Ideen

Test app for the **AI proxy**: `mn.ai.chat` with `gemini-flash` (no key in the browser, cost
counted against the user's monthly budget, 1 € per month for this app). The last five answers
are kept per user in `mn.kv`.

Check: ask for ideas; without provider keys in the AI proxy the app shows a friendly error
instead of breaking.
