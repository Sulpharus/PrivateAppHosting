# Einkauf

Test app for **shared data**: one shopping list for everyone who has the app (`mn.kv` with the
`shared` scope, one key per item). The page refreshes every 5 seconds while it is visible.

Check: add an item as one user, tick it off as another; a user without a grant gets the
"Kein Zugriff" page.
